PORT=3000
NODE_ENV=development

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=maisa-primeris
DB_USER=root
DB_PASS=

# JWT
JWT_SECRET=maisa_primeris_jwt_secret_2026
JWT_EXPIRES_IN=8h


# URL frontend (untuk link di email)
FRONTEND_URL=http://localhost:5173
# SMTP (kalau tidak di-set, link reset hanya di-log ke console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=app-password
SMTP_FROM=noreply@yourdomain.com
LOG_HTTP_BODY=true